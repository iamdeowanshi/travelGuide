<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ithakatales.android" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.IthakaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".ui.activity.test.TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.test.ApiTestActivity"/>
        <activity android:name=".ui.activity.test.ApiTestResultActivity"/>
        <activity android:name=".ui.activity.test.DownloadTourTestActivity"/>
        <activity android:name=".ui.activity.test.MyToursTestActivity"/>

        <service android:name=".download.TourDownloadService"/>

    </application>

</manifest>
